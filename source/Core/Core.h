#ifndef CORE_H
#define CORE_H

#include <QtCore>
#include <QtGui>
#include <QtWidgets>

namespace Jui
{
	enum fade { out, in };
	enum direction { right, bottom, left, top };
	void loadFonts();	
}

#endif // CORE_H